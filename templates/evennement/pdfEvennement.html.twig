<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 100%;
            height: 200px;
            background-color: #f5f5f5;
            color: #333;
            display: flex;
            align-items: center;
            justify-content: space-around;
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .text-frame {
            width: 60%;
            padding: 20px;
        }
        .event {
            font-size: 32px;
            font-weight: bold;
            margin-bottom: 10px;
            color: #333;
        }
        .date {
            font-size: 24px;
            margin-bottom: 20px;
            color: #666;
        }
        .sponsor {
            font-size: 20px;
            color: #666;
        }
        .column-2 img {
            width: 150px;
            height: auto;
            border-radius: 50%;
            border: 5px solid #333;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="text-frame">
            <div class="event">NOM DE L'ÉVÉNEMENT: {{ evennement.getNom() }}</div>
            <div class="date">DATE: {{ evennement.getDateDebut()|date('Y-m-d H:i:s')}} - {{ evennement.getDateFin()|date('Y-m-d H:i:s')}}</div>
            <hr>
            <div class="sponsor">SPONSOR: {{ evennement.getSponsor().getnom() }}</div>
        </div>
        <div class="column-2">
            <img src="{{ asset('assets/img/' ~ evennement.image) }}" alt="Event Image">
        </div>
    </div>
</body>
</html>
